import { IWebsiteRepository } from "@core/interfaces/IWebsiteRepository";

export class MetaTagChecker {
    constructor(private websiteRepository: IWebsiteRepository) {}

    async execute(htmlContent: any){
        
    }
}