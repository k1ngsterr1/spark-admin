export type CartDetails = {
    name: string;
    description: string;
    img_url: string;
    price: number;
}

export type FormDetails = {
    name: string;
    phoneNumber: string;
    email: string;
    date: Date
}